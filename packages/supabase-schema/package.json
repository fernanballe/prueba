{
  "name": "@worlddex/supabase-schema",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist",
    "generate": "supabase gen types typescript --project-id $SUPABASE_PROJECT_ID > src/database.types.ts"
  },
  "dependencies": {
    "@worlddex/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
